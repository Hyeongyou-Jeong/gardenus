{
  "name": "gardenus-functions",
  "private": true,
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && npx firebase-tools emulators:start --only functions",
    "deploy": "npx firebase-tools deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.3.0",
    "openai": "^6.25.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
